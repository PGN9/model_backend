# Core requirements
torch==2.5.0 --index-url https://download.pytorch.org/whl/cpu
transformers==4.39.3
fastapi==0.109.0
uvicorn==0.27.0

# Optional (recommended for production):
pydantic==1.10.15  # Lighter than v2
python-multipart==0.0.6  # Required for file uploads
